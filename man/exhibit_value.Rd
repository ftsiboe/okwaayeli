% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exhibits-workbook.R
\name{exhibit_value}
\alias{exhibit_value}
\title{Extract a Single Value from a Tidy Exhibit Sheet}
\usage{
exhibit_value(data, keys, col)
}
\arguments{
\item{data}{\code{data.frame} from \code{read_exhibit_sheet()}.}

\item{keys}{Named \code{list} of column/value pairs identifying one row, e.g.
\code{list(Equ = "Yield", CropIDx = "Pooled", Coef = "Mean_Pooled")}.}

\item{col}{Character. Name of the column to return (\code{"Beta"}, \code{"SE"},
\code{"Pv"}, \code{"SD"}, \code{"N"}, ...).}
}
\value{
A length-one \code{numeric}, or \code{NA_real_} if no row matches.
}
\description{
Looks up exactly one value from a sheet returned by \code{read_exhibit_sheet()},
given a set of key/value pairs identifying the row.
}
\details{
Returns \code{NA_real_} when no row matches, which lets a table builder leave a
cell blank for a quantity a study does not estimate. \strong{Errors} when more
than one row matches: a duplicate key means the sheet is malformed, and
silently taking the first match is how a lookup returns a plausible but wrong
number. See the \emph{Duplicate rows} section of \code{read_exhibit_sheet()}.
}
\examples{
\dontrun{
m <- read_exhibit_sheet("studies/land_tenure/output/land_tenure_results.xlsx")
exhibit_value(m, list(Equ = "Yield", CropIDx = "Pooled",
                      Coef = "Mean_Pooled"), "Beta")
}

}
\seealso{
\code{read_exhibit_sheet()}

Other exhibits:
\code{\link[=exhibit_cell]{exhibit_cell()}},
\code{\link[=exhibit_crop_table]{exhibit_crop_table()}},
\code{\link[=exhibit_group_sizes]{exhibit_group_sizes()}},
\code{\link[=exhibit_sheet_to_schema]{exhibit_sheet_to_schema()}},
\code{\link[=exhibit_stars]{exhibit_stars()}},
\code{\link[=exhibit_wave_table]{exhibit_wave_table()}},
\code{\link[=read_exhibit_sheet]{read_exhibit_sheet()}}
}
\concept{exhibits}
