% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exhibits-figures-modern001.R
\name{fig_modern001_speccurve}
\alias{fig_modern001_speccurve}
\title{Specification curve}
\usage{
fig_modern001_speccurve(
  estimates,
  estimate = "Estimate",
  se = "Estimate.sd",
  p = "jack_pv",
  id = "spec",
  choices = NULL,
  choice = "choice",
  family = NULL,
  reference = NULL,
  reference_label = "Preferred specification",
  strip_position = c("left", "top"),
  y_title = "Estimate",
  base_size = 9,
  ci = 1.96,
  heights = c(2, 1.4)
)
}
\arguments{
\item{estimates}{A data frame, one row per specification.}

\item{estimate, se, p}{Column names in \code{estimates}.}

\item{id}{Column name holding the specification identifier, used to join to
\code{choices}.}

\item{choices}{Optional long data frame with one row per specification and
active choice, holding \code{id}, a \code{choice} label and optionally a \code{family}
grouping. \code{NULL} returns the upper panel alone.}

\item{choice, family}{Column names within \code{choices}.}

\item{reference}{Optional value to mark with a horizontal rule, typically the
preferred specification's estimate.}

\item{reference_label}{Label for that rule.}

\item{strip_position}{Where the family heading sits, on the same terms as
\code{fig_modern001_grid()}. \code{"left"} places it outside the axis, horizontal,
left-aligned and bold. \code{"top"} gives each family its own sub-panel headed
above, stacked under the curve. Both keep the lower panel column-aligned
with the curve, because every panel is drawn on one explicit shared x scale
rather than on whatever range its own rows happen to span.}

\item{y_title}{Axis title for the upper panel.}

\item{base_size}{Base font size.}

\item{ci}{Interval multiplier.}

\item{heights}{Relative heights of the two panels.}
}
\value{
A list with \code{estimates}, \code{choices} and \code{plot}. \code{choices} is a single
ggplot under \code{strip_position = "left"} and a named list of one ggplot per
family under \code{"top"}, so either can be restyled before assembly. \code{plot} is
assembled with \code{cowplot} when it is available and is \code{NULL} otherwise.
}
\description{
Every specification a robustness arm estimates, sorted by result, over a
panel showing which analytical choices produced each one. It answers the only
question a robustness exhibit should: does the finding survive the whole space
of defensible choices, and how many estimates cross zero.
}
\details{
Returns the two panels separately as well as an assembled plot, so a caller
who wants to restyle one panel can.
}
\seealso{
Other modern001 figure helpers: 
\code{\link{fig_modern001_balance}()},
\code{\link{fig_modern001_check}()},
\code{\link{fig_modern001_decomposition}()},
\code{\link{fig_modern001_dumbbell}()},
\code{\link{fig_modern001_fmt}()},
\code{\link{fig_modern001_grid}()},
\code{\link{fig_modern001_palette}()},
\code{\link{fig_modern001_points}()},
\code{\link{fig_modern001_prepare}()},
\code{\link{fig_modern001_reference}()},
\code{\link{fig_modern001_save}()},
\code{\link{fig_modern001_scales}()},
\code{\link{fig_modern001_significant}()},
\code{\link{fig_modern001_slope}()},
\code{\link{fig_modern001_style}()},
\code{\link{fig_modern001_theme}()}
}
\concept{modern001 figure helpers}
