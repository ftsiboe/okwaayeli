% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptive-exhibits-core.R
\name{descriptive_prepare}
\alias{descriptive_prepare}
\title{Add the Trend and Cluster Columns Used by the Descriptive Engines}
\usage{
descriptive_prepare(
  data,
  season_var = "Season",
  cluster_vars = c("Survey", "Ecozon", "EaId", "HhId")
)
}
\arguments{
\item{data}{\code{data.frame} subset (normally one crop).}

\item{season_var}{Character. Default \code{"Season"}. Numeric years, or a
factor/character whose labels begin with a four-digit year.}

\item{cluster_vars}{Character vector. Default
\code{c("Survey","Ecozon","EaId","HhId")}.}
}
\value{
\code{data} with \code{.trend} (years since the subset's first wave) and
\code{.clust} added.
}
\description{
Reproduces the two derived columns every \verb{100_*.do} builds before estimating:
\code{Trend = Season - min(Season)} and a cluster id grouping
\verb{Survey Ecozon EaId HhId}.
}
\details{
\code{Trend} is computed \strong{within the subset passed in}, matching the Stata code,
which recomputes \verb{sum Season} / \code{r(min)} after \verb{keep if CropIDx == "<crop>"}.
A crop whose earliest observation is not GLSS3 therefore has its trend origin
at its own first wave, not the study's. This is faithful, not incidental --
change it only deliberately.
}
\section{Season must be a year, not a factor code}{

\code{Season} arrives from \code{get_household_data()} as an R \strong{factor}, because the
harmonized \code{.dta} labels it and \code{haven::as_factor()} is applied on read. Its
levels span the whole GLSS series:

\preformatted{
1987/88 1988/89 1990/91 1991/92 1997/98 1998/99 2004/05 2005/06
2009/10 2011/12 2012/13 2013/14 2014/15 2015/16 2016/17
}

\code{as.numeric()} on that returns the \strong{level code}, not the year. For
resource_extraction the four waves sit at codes 5, 6, 7, 8, 10, 11, 14, 15, so
a naive conversion yields a trend of \verb{0,1,2,3,5,6,9,10} where Stata --- whose
\code{Season} is the numeric year --- gets \verb{0,1,7,8,14,15,18,19}. The trend
coefficient then comes out roughly twice too large, and every "annual
percentage change" with it. The error is silent: the numbers look reasonable.

This function therefore parses the leading four-digit year out of the label
(\code{"1997/98" -> 1997}) whenever \code{Season} is not already numeric, so the unit of
\code{.trend} is a year and the exhibits' "annual" label is true.
}

\seealso{
Other descriptive exhibits:
\code{\link[=descriptive_expand_category]{descriptive_expand_category()}},
\code{\link[=descriptive_group_summary]{descriptive_group_summary()}},
\code{\link[=descriptive_indicator_shares]{descriptive_indicator_shares()}},
\code{\link[=descriptive_season_year]{descriptive_season_year()}},
\code{\link[=descriptive_specifications]{descriptive_specifications()}},
\code{\link[=descriptive_trend_model]{descriptive_trend_model()}},
\code{\link[=descriptive_workhorse]{descriptive_workhorse()}},
\code{\link[=draw_descriptive_summary]{draw_descriptive_summary()}}
}
\concept{descriptive exhibits}
