% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptive-exhibits-core.R
\name{descriptive_group_summary}
\alias{descriptive_group_summary}
\title{Weighted Summary Statistics by Group}
\usage{
descriptive_group_summary(
  data,
  outcome,
  treatment = NULL,
  wave_var = "Surveyx",
  weights = NULL
)
}
\arguments{
\item{data}{\code{data.frame}, already subset to one crop.}

\item{outcome}{Character. Outcome column.}

\item{treatment}{Character or \code{NULL}. Binary treatment column. \code{NULL} emits
pooled rows only (Engine B).}

\item{wave_var}{Character. Default \code{"Surveyx"}.}

\item{weights}{Character or \code{NULL}. Column of sampling weights.}
}
\value{
A \code{data.frame} with \code{wave}, \code{group}, \code{statistic = "mean"},
\code{estimate}, \code{se}, \code{min}, \code{max}, \code{sd}, \code{n}.
}
\description{
Engine A's aggregation step: mean, standard error of the mean, min, max,
standard deviation and n, computed overall and by treatment group, and again
within each survey wave. Replaces Stata's \verb{tabstat ..., stat(mean sem min max sd n) by()}.
}
\details{
With \code{weights = NULL} (default) this reproduces the Stata path exactly: no
\verb{100_*.do} in the repository applies sampling weights, despite \code{WeightHH}
being available. Supplying \code{weights = "WeightHH"} yields
population-representative means; see the \emph{Divergences} section of
\code{studies/README_descriptive_exhibits_plan.md}.
}
\section{Rows with a missing treatment are dropped}{

When \code{treatment} is supplied, \strong{every} row it emits -- including the pooled
row -- is restricted to observations with a non-missing treatment. This
matches \verb{tabstat <var>, by(<treatment>)}, which excludes observations whose
by-variable is missing, and it keeps the pooled row on the same sample as the
group rows.

It matters. In resource_extraction the commercial/informal mining split exists
only in GLSS6/GLSS7, so \code{mining_comm} and \code{mining_gala} are missing for all
10,416 GLSS4/GLSS5 observations. Pooling over every row regardless gives
n = 26,811 against Stata's 16,395, and a mean drawn from a different
population than the groups it sits beside.
}

\seealso{
Other descriptive exhibits: 
\code{\link{descriptive_expand_category}()},
\code{\link{descriptive_indicator_shares}()},
\code{\link{descriptive_prepare}()},
\code{\link{descriptive_season_year}()},
\code{\link{descriptive_specifications}()},
\code{\link{descriptive_trend_model}()},
\code{\link{descriptive_workhorse}()},
\code{\link{draw_descriptive_summary}()}
}
\concept{descriptive exhibits}
