% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exhibits-workbook.R
\name{exhibit_crop_table}
\alias{exhibit_crop_table}
\title{Build a Crop-by-Family Headcount Table}
\usage{
exhibit_crop_table(
  sheet,
  variables,
  crops,
  measure = "GLSS0",
  digits = 3,
  pooled_key = "Pooled",
  pooled_label = "All crops listed"
)
}
\arguments{
\item{sheet}{\code{data.frame} from \code{read_exhibit_sheet(path, "<study>")}.}

\item{variables}{Character vector naming the family members, in column order
(e.g. \code{c("LndOwn_1", "LndOwn_2", "LndOwn_3")}).}

\item{crops}{Character vector of crops, in row order. The pooled key is
appended automatically and must not be included.}

\item{measure}{Character. Value of \code{mesure} to read. Default \code{"GLSS0"}.}

\item{digits}{Integer. Decimal places. Default 3.}

\item{pooled_key}{Character. Value of \code{crop} holding the pooled row.}

\item{pooled_label}{Character. Row label for the pooled row.}
}
\value{
A \code{data.frame} with \code{label}, \code{header} and \code{c1}..\code{cN}, ready for a
flextable builder.
}
\description{
Builds the appendix analogues of the main tenure/exposure table: one row per
crop, one column per member of a variable family, each cell a mean with its
standard deviation. This is the shape of the land tenure study's Tables S1--S4
and of the equivalent crop-disaggregated appendix tables in the other studies.
}
\details{
These tables are the \emph{same sheet} as the pooled main table, sliced by crop
instead of restricted to \code{"Pooled"}. In the land tenure study:

\tabular{lll}{
\strong{Table} \tab \strong{\code{variables}} \tab \strong{Content} \cr
S1 \tab \code{LndOwn_1..3}    \tab ownership status \cr
S2 \tab \code{LndAq_1..5}     \tab mode of acquisition \cr
S3 \tab \code{LndRgt_1..4}    \tab ownership rights \cr
S4 \tab \code{ShrCrpCat_1..3} \tab sharecropping intensity
}

The pooled row is emitted last, labelled \code{pooled_label}, and is the same
\code{crop == pooled_key} row the main table reports.

\code{measure} defaults to \code{"GLSS0"}, the headcount pooled across the waves in
which the module was administered, which is what the appendix tables report.
}
\seealso{
\code{\link[=read_exhibit_sheet]{read_exhibit_sheet()}}, \code{\link[=exhibit_wave_table]{exhibit_wave_table()}}

Other exhibits: 
\code{\link{exhibit_cell}()},
\code{\link{exhibit_group_sizes}()},
\code{\link{exhibit_sheet_to_schema}()},
\code{\link{exhibit_stars}()},
\code{\link{exhibit_value}()},
\code{\link{exhibit_wave_table}()},
\code{\link{read_exhibit_sheet}()}
}
\concept{exhibits}
