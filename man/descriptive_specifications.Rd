% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptive-exhibits-core.R
\name{descriptive_specifications}
\alias{descriptive_specifications}
\title{Build a Descriptive Exhibit Specification Grid}
\usage{
descriptive_specifications(
  data,
  outcomes,
  treatments,
  crops = NULL,
  crop_var = "CropID",
  families = "gaussian"
)
}
\arguments{
\item{data}{\code{data.frame} of the merged analysis sample, normally
\code{study_environment$study_raw_data}.}

\item{outcomes}{Character vector of outcome columns (Engine A), e.g.
\code{c("Yield","Area","SeedKg", ...)}.}

\item{treatments}{Character vector of binary treatment columns, e.g.
\code{"OwnLnd"} or \code{c("extraction_any","mining_any", ...)}.}

\item{crops}{Character vector of crops to enumerate, or \code{NULL} (default) to
take every level present in \code{crop_var}.}

\item{crop_var}{Character. Column holding the crop. Default \code{"CropID"}.}

\item{families}{Either a single family applied to every outcome
(\code{"gaussian"}, the default), or a \strong{named} character vector mapping outcome
to family. See \emph{Outcomes do not all share a model}.}
}
\value{
A \code{data.frame} with columns \code{treatment}, \code{crop}, \code{outcome}, \code{family}.
}
\description{
Enumerates the (treatment x crop x outcome) combinations a study's descriptive
exhibits require. Mirrors \code{sf_model_specifications()} for the frontier layer.
}
\details{
The land tenure study's \verb{100_exhibits.do} hardcodes \code{i.OwnLnd} at every site
and has no treatment loop, which is why it cannot vary its treatment without
editing code. Every other study generalises to a \code{disagCat} column. This
function makes the loop a data structure, so a study varies its treatment by
passing a longer \code{treatments} vector.
}
\section{Outcomes do not all share a model}{

A study's Table 1 is usually estimated with more than one model, and the
do-files express that as separate loops. In \verb{land_tenure/scripts/100_exhibits.do}:

\preformatted{
line  51  foreach Var of var Yield Area SeedKg HHLaborAE HirdHr FertKg PestLt
                             AgeYr YerEdu HHSizeAE Depend CrpMix
          -> reg `Var' c.Trend##i.OwnLnd            (gaussian)

line 103  foreach Var of var Female EqipMech Credit Extension EqipIrig
          -> logit `Var' c.Trend##i.OwnLnd          (binomial)
}

Both write to the same sheet, so the distinction is invisible downstream --
\code{Equ == "Female"} and \code{Equ == "Yield"} sit side by side with no record that one
came from a logit. Passing a named \code{families} vector makes it explicit and
carries it into the emitted schema:

\preformatted{
descriptive_specifications(
  d, outcomes = c(cont, bin), treatments = "OwnLnd",
  families = c(setNames(rep("gaussian", length(cont)), cont),
               setNames(rep("binomial",  length(bin)),  bin)))
}
}

\seealso{
Other descriptive exhibits: 
\code{\link{descriptive_expand_category}()},
\code{\link{descriptive_group_summary}()},
\code{\link{descriptive_indicator_shares}()},
\code{\link{descriptive_prepare}()},
\code{\link{descriptive_season_year}()},
\code{\link{descriptive_trend_model}()},
\code{\link{descriptive_workhorse}()},
\code{\link{draw_descriptive_summary}()}
}
\concept{descriptive exhibits}
