% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study_setup.R
\name{.study_layouts}
\alias{.study_layouts}
\title{Study output layouts}
\usage{
.study_layouts()
}
\value{
Named list of layouts, each a list of \code{figures}, \code{figure_data} and
\code{tables} directory names.
}
\description{
The directory names each layout uses, relative to \code{wd$output}.
}
\details{
Two layouts exist because the studies migrated at different times:

\describe{
\item{\code{legacy}}{\verb{figure/} for plots, \verb{figure_data/} for the data behind
them. Used by ag_services, disability, education, financial_inclusion,
input_dealers, resource_extraction and time_poverty.}
\item{\code{v2}}{\verb{figures/} for plots AND the data behind them -- one folder per
concept, so a \code{.png} and its \code{.csv} sit together -- plus \verb{tables/} for
table data. Used by land_tenure since 2026-07-16.}
}

\verb{tables/} exists in both: nothing wrote table data before, so there is no
legacy name to preserve.
}
\keyword{internal}
