% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exhibits-figures-modern001.R
\name{fig_modern001_grid}
\alias{fig_modern001_grid}
\title{Small-multiple grid: categories down, measures across}
\usage{
fig_modern001_grid(
  data,
  estimate = "Estimate",
  se = "Estimate.sd",
  p = "jack_pv",
  group = "group",
  measure = "measure",
  block = NULL,
  measures = NULL,
  order_by = NULL,
  free_y = TRUE,
  strip_position = c("left", "top"),
  x_title = "Treated minus untreated",
  base_size = 9,
  ci = 1.96,
  scale = 1
)
}
\arguments{
\item{data}{A tidy data frame, one row per category and measure.}

\item{estimate, se, p, group, measure}{Column names.}

\item{block}{Optional column name grouping categories into labelled panels,
for example gender, age and schooling in one figure.}

\item{measures}{Character vector fixing measure order and column order.}

\item{order_by}{Measure label to sort categories by within each block.}

\item{free_y}{Give each block only its own categories. Almost always \code{TRUE}
when \code{block} is used.}

\item{strip_position}{Where the block heading sits. \code{"left"} places it
outside the axis, horizontal, left-aligned and bold, alongside its rows.
\code{"top"} stacks the blocks and puts the heading above each one, which suits
long category labels that would otherwise push a left heading far off the
plot. \code{"top"} needs cowplot and falls back to \code{"left"} with a message if it
is unavailable.}

\item{x_title}{Axis title.}

\item{base_size}{Base font size.}

\item{ci}{Interval multiplier. Set to \code{0} to draw points alone, which is
right when the underlying estimates carry no standard error.}

\item{scale}{Multiplier applied to estimates.}
}
\value{
A ggplot object.
}
\description{
For heterogeneity, where the categories are too many for colour to carry
identity. Identity moves to the row label and colour is freed to encode the
measure, one per column. Past roughly seven categories this is the honest
form; adding hues is not.
}
\seealso{
Other modern001 figure helpers: 
\code{\link{fig_modern001_balance}()},
\code{\link{fig_modern001_check}()},
\code{\link{fig_modern001_decomposition}()},
\code{\link{fig_modern001_dumbbell}()},
\code{\link{fig_modern001_fmt}()},
\code{\link{fig_modern001_palette}()},
\code{\link{fig_modern001_points}()},
\code{\link{fig_modern001_prepare}()},
\code{\link{fig_modern001_reference}()},
\code{\link{fig_modern001_save}()},
\code{\link{fig_modern001_scales}()},
\code{\link{fig_modern001_significant}()},
\code{\link{fig_modern001_slope}()},
\code{\link{fig_modern001_speccurve}()},
\code{\link{fig_modern001_style}()},
\code{\link{fig_modern001_theme}()}
}
\concept{modern001 figure helpers}
