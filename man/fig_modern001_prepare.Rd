% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exhibits-figures-modern001.R
\name{fig_modern001_prepare}
\alias{fig_modern001_prepare}
\title{Normalise a study's estimate table for the modern001 builders}
\usage{
fig_modern001_prepare(
  data,
  estimate = "Estimate",
  se = "Estimate.sd",
  p = "jack_pv",
  group = NULL,
  measure = NULL,
  block = NULL,
  measures = NULL,
  groups = NULL,
  order_by = NULL,
  level = 0.05,
  ci = 1.96,
  scale = 1
)
}
\arguments{
\item{data}{A data frame of estimates.}

\item{estimate, se, p}{Column names holding the point estimate, its standard
error and its p-value. \code{p} may be \code{NULL}.}

\item{group}{Column name holding the row category, for example the treatment
arm, the survey round or the crop.}

\item{measure}{Column name holding the measure. \code{NULL} for a single-measure
figure, in which case one slot is used.}

\item{block}{Optional column name holding a panel grouping, used by
\code{fig_modern001_grid()}.}

\item{measures}{Optional character vector fixing the order of measure levels.
Defaults to order of appearance.}

\item{groups}{Optional character vector fixing the order of group levels.
Defaults to order of appearance. Use \code{order_by} instead to sort by value.}

\item{order_by}{Optional measure label. When supplied, groups are ordered by
that measure's estimate, which is how a ranked figure is produced without
the caller reshaping anything.}

\item{level}{Significance level for the filled or hollow decision.}

\item{ci}{Interval multiplier. Default 1.96, a 95 percent normal interval.
Note that the older \verb{fig_*()} builders in this package draw plus or minus
one standard error; this system draws a 95 percent interval, which is what
a reader assumes an error bar means.}

\item{scale}{Multiply estimates and errors by this before plotting. Use 100
to report percentage points.}
}
\value{
A data frame carrying \code{.est}, \code{.se}, \code{.p}, \code{.sig}, \code{.lo}, \code{.hi},
\code{.grp}, \code{.msr} and, where requested, \code{.blk}.
}
\description{
Renames whichever columns a study happens to use into the fixed internal
names every builder expects, computes the interval and the significance flag,
and fixes the factor order of the measures so palette slots are assigned in
the order given rather than alphabetically.
}
\details{
Call this directly only when composing a figure by hand from the primitives.
The builders call it themselves.
}
\seealso{
Other modern001 figure helpers:
\code{\link[=fig_modern001_balance]{fig_modern001_balance()}},
\code{\link[=fig_modern001_check]{fig_modern001_check()}},
\code{\link[=fig_modern001_decomposition]{fig_modern001_decomposition()}},
\code{\link[=fig_modern001_dumbbell]{fig_modern001_dumbbell()}},
\code{\link[=fig_modern001_fmt]{fig_modern001_fmt()}},
\code{\link[=fig_modern001_grid]{fig_modern001_grid()}},
\code{\link[=fig_modern001_palette]{fig_modern001_palette()}},
\code{\link[=fig_modern001_points]{fig_modern001_points()}},
\code{\link[=fig_modern001_reference]{fig_modern001_reference()}},
\code{\link[=fig_modern001_save]{fig_modern001_save()}},
\code{\link[=fig_modern001_scales]{fig_modern001_scales()}},
\code{\link[=fig_modern001_significant]{fig_modern001_significant()}},
\code{\link[=fig_modern001_slope]{fig_modern001_slope()}},
\code{\link[=fig_modern001_speccurve]{fig_modern001_speccurve()}},
\code{\link[=fig_modern001_style]{fig_modern001_style()}},
\code{\link[=fig_modern001_theme]{fig_modern001_theme()}}
}
\concept{modern001 figure helpers}
