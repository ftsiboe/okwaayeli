% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptive-exhibits-core.R
\name{descriptive_trend_model}
\alias{descriptive_trend_model}
\title{Trend and Difference Tests for One Outcome}
\usage{
descriptive_trend_model(
  data,
  outcome,
  treatment = NULL,
  family = c("gaussian", "binomial"),
  weights = NULL,
  ssc = NULL,
  min_events = 10L,
  min_group_n = 30L
)
}
\arguments{
\item{data}{\code{data.frame} from \code{\link[=descriptive_prepare]{descriptive_prepare()}}, subset to one crop.}

\item{outcome}{Character. Outcome column.}

\item{treatment}{Character or \code{NULL}. Binary treatment column.}

\item{family}{\code{"gaussian"} (default) or \code{"binomial"} for a dummy outcome.}

\item{weights}{Character or \code{NULL}.}

\item{ssc}{\code{fixest::ssc()} object controlling the small-sample correction.
Defaults to \code{NULL} (fixest default). Validated against resource_extraction
2026-07-15 with no tuning needed: point estimates and \code{testparm} p-values
both matched Stata.}

\item{min_events}{Integer. For \code{family = "binomial"}, the minimum number of
observations in the rarer class -- overall and within each treatment group --
below which no model is fitted and \code{NULL} is returned. Guards against the
near-separation fits that produce nonsense trends for rare indicators inside
small crops. Default 10.}

\item{min_group_n}{Integer. Minimum observations in the smaller treatment
group, for either family. Default 30.}
}
\value{
A \code{data.frame} with \code{wave = "all"}, \code{group}, \code{statistic}, \code{estimate},
\code{se}, \code{t}, \code{p}; or \code{NULL} if the data cannot support the model.
}
\description{
Engine A's estimation step. Fits \code{outcome ~ trend * treatment} with clustered
standard errors and returns the per-group percentage trend plus the two Wald
tests the exhibits report. Replaces Stata's
\verb{reg y c.Trend##i.g, vce(cluster C)} + \code{testparm} + \verb{margins ... eydx} +
\code{nlcom (... * 100)}.
}
\details{
Emitted statistics:

\describe{
\item{\code{trend_pct}}{Average semi-elasticity of \code{outcome} with respect to
\code{trend}, times 100 -- i.e. the annual percentage change. Stata computes
this as \verb{margins <g>, eydx(Trend)} followed by \verb{nlcom (_b[...] * 100)};
here it is \code{\link[marginaleffects:slopes]{marginaleffects::avg_slopes()}} with \code{slope = "eydx"}. Emitted
per group and pooled.}
\item{\code{cat_diff}}{\verb{testparm i.g} -- the treatment main effect, i.e. the
group level difference \strong{at \code{trend == 0}}, not the difference in overall
means. This is why an outcome can show a large raw gap yet an
insignificant \code{cat_diff}.}
\item{\code{trend_diff}}{\verb{testparm c.Trend#i.g} -- the interaction.}
}

Only \code{p} is populated for the two Wald rows, matching the sheet, where
\code{CATDif} / \code{TrendDif} carry a p-value and nothing else.
}
\section{Refusing an unestimable trend}{

A trend is only fitted where the data can carry it. Both guards return \code{NULL}
rather than a number, and \code{\link[=descriptive_workhorse]{descriptive_workhorse()}} logs the refusal.

This is a deliberate divergence from the Stata path, and the only one in this
file. Every \verb{100_*.do} wraps its estimation in \verb{cap\{ \}}, which swallows a
failed fit and leaves the exported cell at whatever it held. The consequences
are in the shipped workbooks. From resource_extraction's own sheets on
2026-07-15:

\preformatted{
Pineapple Depend Trend_disagCat1   -669,487,338,753,097,984  (percent/year)
Pineapple FertKg Trend_disagCat1   -153,054,440,947,974,016
Onion     PestLt Trend_disagCat1      8,109,369,589,760
salt      x Tomatoe  Trend                              0    (fit failed)
}

Those are collapsed fits recorded as estimates. Reproducing them would be
faithful and wrong: an annual percentage change of -6.7e17 is not a finding,
and a crop-by-treatment trend estimated off a handful of farms should not be
printed at all. Where a cell is genuinely estimable this function matches
Stata exactly (14,724 parity assertions, both engines, 7 treatments, 28 crops).

Raise \code{min_group_n} / \code{min_events} to be stricter, or lower them to 0 to
reproduce the Stata behaviour including its failures.
}

\seealso{
Other descriptive exhibits: 
\code{\link{descriptive_expand_category}()},
\code{\link{descriptive_group_summary}()},
\code{\link{descriptive_indicator_shares}()},
\code{\link{descriptive_prepare}()},
\code{\link{descriptive_season_year}()},
\code{\link{descriptive_specifications}()},
\code{\link{descriptive_workhorse}()},
\code{\link{draw_descriptive_summary}()}
}
\concept{descriptive exhibits}
