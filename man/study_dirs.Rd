% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study_setup.R
\name{study_dirs}
\alias{study_dirs}
\title{Repair and create a study's directories}
\usage{
study_dirs(
  study_environment = NULL,
  project_name = NULL,
  layout = NULL,
  create = TRUE
)
}
\arguments{
\item{study_environment}{A study environment from \code{study_setup()}, or \code{NULL}
to build a fresh \code{wd} from \code{project_name} alone.}

\item{project_name}{Length-1 character. Defaults to
\code{study_environment$project_name}, then to the basename inferred from
\code{wd$home}.}

\item{layout}{Optional layout override. See \code{.study_layouts()}. Defaults to
\code{study_environment$layout}, then \code{"legacy"}.}

\item{create}{Create the directories? Default \code{TRUE}.}
}
\value{
\code{study_environment} with \code{wd}, \code{project_name} and \code{layout} repaired.
}
\description{
Backfills \code{study_environment$wd} from \code{project_name} and creates every
directory it names. Returns the repaired environment. This is the single
definition of the directory tree; \code{study_setup()} is a thin wrapper over it.
}
\section{Why this exists}{

\code{wd} is written into \verb{<project>_study_environment.rds} by \code{study_setup()},
which only \code{000}/\code{001} call, so it is a \emph{frozen snapshot}: a directory added
to the layout does not reach later stages until the environment is
regenerated -- for most studies, an expensive re-run of matching. A stage
reading an older \code{.rds} gets \code{NULL} for a new entry, and
\code{file.path(NULL, "x.rds")} returns \code{character(0)} rather than erroring, which
surfaces far from its cause as a \code{gzfile()} connection failure.

Calling \code{study_dirs()} after \code{readRDS()} makes \code{wd} advisory: paths are
recomputed from \code{project_name}, missing entries filled, folders created.
}

\examples{
\dontrun{
se <- readRDS("studies/land_tenure/data/land_tenure_study_environment.rds")
se <- study_dirs(se, layout = "v2")
ggplot2::ggsave(file.path(study_dir_figures(se), "trend.png"), fig)
}
}
\seealso{
\code{study_setup()}, \code{study_dir_figures()}, \code{study_dir_tables()}
}
