% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exhibits-workbook.R
\name{read_exhibit_sheet}
\alias{read_exhibit_sheet}
\title{Read a Tidy Exhibit Sheet from a Study Results Workbook}
\usage{
read_exhibit_sheet(path, sheet = "means", layout = c("auto", "means", "study"))
}
\arguments{
\item{path}{Character. Path to the \code{.xlsx} results workbook.}

\item{sheet}{Character. Sheet name. See \emph{Sheet naming across studies}.}

\item{layout}{Character. Which column naming to apply. \code{"auto"} (default)
detects from the sheet's own headers; \code{"means"} and \code{"study"} force it.}
}
\value{
A \code{data.frame} with eleven canonically named columns.
}
\description{
Reads one of the machine-readable ("tidy") sheets written by a study's
\verb{100_exhibits.do} / \verb{100_FIGTAB_*.do} into a \code{data.frame}, drops the
display-formula spill columns, and applies the canonical column names.
}
\details{
Each study's results workbook carries two kinds of sheet:
\itemize{
\item \strong{Tidy value sheets}, written by Stata's
\verb{export excel ... firstrow(variables)}. These contain values, one row per
estimated quantity, and are the source of truth for the manuscript tables.
\item \strong{Display sheets} (\code{Table1}, \code{Table2}, ...), hand-built with formulas that
reference the tidy sheets. These cache \verb{#N/A} once Stata rewrites the tidy
data with \code{sheetmodify} and must not be read.
}

Only the first eleven columns of a tidy sheet are real; anything beyond is
spill from adjacent display formulas (read by \pkg{readxl} as \code{...12}, \verb{***},
\code{0.00 (0.00)} and similar) and is discarded.

The two recognised layouts are:

\preformatted{
means   CropIDx, Equ, Coef, Beta, SE, Tv, Pv, Min, Max, SD, N
<study> Variable, crop, mesure, Beta, SE, Tv, Pv, Min, Max, SD, N
}

In the \code{means} layout, \code{Equ} is the outcome variable, \code{Coef} the estimated
quantity (\verb{Mean_<group>}, \verb{Trend_<group>}, \code{CATDif}, \code{TrendDif},
\verb{<wave>_<group>}), and \code{CropIDx} the crop (\code{"Pooled"} for all crops).
}
\section{Duplicate rows}{

Six of the seven study do-files shipped a bug in which \verb{mat roweq A = Female}
was hardcoded inside the loop over the treatment dummies, so those rows were
written to the \code{means} sheet tagged \code{Equ == "Female"} and collided with the
real \code{Female} outcome rows. Fixed 2026-07-15 (\verb{mat roweq A = \\}Var'\verb{). Sheets produced before that fix contain duplicate }(Equ, CropIDx, Coef)` keys;
\code{\link[=exhibit_value]{exhibit_value()}} errors on them rather than silently returning the first
match.
}

\section{Sheet naming across studies}{

Sheet names are \strong{not} consistent, because each study's do-file names them
from its own treatment loop:

\tabular{lll}{
\strong{Study} \tab \strong{Engine A sheet(s)} \tab \strong{Engine B sheet} \cr
land_tenure \tab \code{means} (one treatment) \tab \code{land_tenure} \cr
resource_extraction \tab \code{Means_extraction_any}, \code{Means_mining_any},
\code{Means_mining_comm}, \code{Means_mining_gala}, \code{Means_quarrying},
\code{Means_sand}, \code{Means_salt} \tab \code{extraction}
}

\code{layout = "auto"} therefore does \strong{not} key off the sheet name. It inspects
the first three column headers as written by Stata (\verb{CropIDx, Equ, Coef} for
Engine A; \verb{Variable, crop, mesure} for Engine B) and picks accordingly. Keying
off the name would silently apply the wrong column names to
\code{Means_extraction_any}.
}

\seealso{
\code{\link[=exhibit_value]{exhibit_value()}}, \code{\link[=exhibit_stars]{exhibit_stars()}}, \code{\link[=exhibit_group_sizes]{exhibit_group_sizes()}}

Other exhibits: 
\code{\link{exhibit_cell}()},
\code{\link{exhibit_crop_table}()},
\code{\link{exhibit_group_sizes}()},
\code{\link{exhibit_stars}()},
\code{\link{exhibit_value}()},
\code{\link{exhibit_wave_table}()}
}
\concept{exhibits}
