# `literature/`

Source PDFs for the manuscript's citation base — the same role this folder plays
in `land_tenure` and `resource_extraction`.

Empty. This README exists so the folder is tracked; git does not track empty
directories, and the folder needs to exist before `narrative/references.bib` has
anything in it.

Nothing reads this folder programmatically. It is for reading.
